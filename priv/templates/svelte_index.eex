<!-- GENERATED by Caravela from <%= @domain_module %> — edit below the CUSTOM marker. -->
<script lang="ts">
  import type { <%= @entity_ts %> } from '<%= @types_import %>';

  // Props pushed from LiveView via LiveSvelte.
  // `pushEvent` dispatches events back to LiveView.
  let {
    <%= @plural %> = [],
    loading = false,
    flash_message = null,
    pushEvent
  }: {
    <%= @plural %>?: <%= @entity_ts %>[];
    loading?: boolean;
    flash_message?: string | null;
    pushEvent: (event: string, payload: object) => void;
  } = $props();

  function handleDelete(id: string) {
    if (confirm('Delete this <%= @singular %>?')) {
      pushEvent('delete', { id });
    }
  }
</script>

{#if flash_message}
  <div class="flash">{flash_message}</div>
{/if}

<div class="toolbar">
  <button onclick={() => pushEvent('new', {})}>New <%= @singular %></button>
</div>

{#if loading}
  <div class="loading">Loading…</div>
{:else if <%= @plural %>.length === 0}
  <div class="empty">No <%= @plural %> yet.</div>
{:else}
  <table>
    <thead>
      <tr>
<%= for col <- @columns do %>        <th><%= col.label %></th>
<% end %>        <th></th>
      </tr>
    </thead>
    <tbody>
      {#each <%= @plural %> as <%= @singular %> (<%= @singular %>.id)}
        <tr>
<%= for col <- @columns do %>          <td><%= col.cell %></td>
<% end %>          <td>
            <button onclick={() => pushEvent('edit', { id: <%= @singular %>.id })}>Edit</button>
            <button onclick={() => handleDelete(<%= @singular %>.id)}>Delete</button>
          </td>
        </tr>
      {/each}
    </tbody>
  </table>
{/if}

<!-- --- CUSTOM --- -->
