// GENERATED by Caravela from <%= @domain_module %> - edit below the CUSTOM marker.
// @caravela-entity <%= @entity_ts %> @caravela-mode <%= @frontend %> @caravela-test show
import { describe, expect, test } from 'vitest';
import { render } from '@testing-library/svelte';
import <%= @component_name %> from './<%= @component_name %>.svelte';

describe('<%= @component_name %>', () => {
  test('mounts with a single entity', () => {
    const { container } = render(<%= @component_name %>, {
      props: {
        <%= @singular %>: { id: '1' } as any,
        field_access: <%= @default_field_access %>,
      }
    });

    expect(container).toBeTruthy();
    // TODO: assert a known field value is visible.
  });
});
